# 📊 Инструкция: Работа с базой данных напрямую

## 🎯 Назначение

Эта инструкция поможет вам редактировать данные бота напрямую через базу данных SQLite, минуя интерфейс бота.

## ⚠️ Важно!

**Перед редактированием базы данных:**
1. ✅ Остановите бота (`Ctrl+C` в терминале)
2. ✅ Сделайте резервную копию: `copy bot_database.db bot_database_backup.db`
3. ✅ После изменений перезапустите бота

## 🛠️ Инструменты для работы с базой

### Вариант 1: DB Browser for SQLite (Рекомендуется)
**Скачать:** https://sqlitebrowser.org/

**Преимущества:**
- ✅ Графический интерфейс
- ✅ Простое редактирование
- ✅ Визуализация данных
- ✅ Безопасно

**Как использовать:**
1. Скачайте и установите DB Browser
2. Откройте `bot_database.db`
3. Вкладка "Browse Data" → выберите таблицу
4. Редактируйте данные
5. Сохраните (`Ctrl+S`)

### Вариант 2: Командная строка SQLite

```bash
# Открыть базу данных
sqlite3 bot_database.db

# Посмотреть список таблиц
.tables

# Посмотреть структуру таблицы
.schema upload_stats

# Выйти
.quit
```

### Вариант 3: Python скрипт

Создайте файл `edit_db.py`:

```python
import sqlite3

# Подключение к базе
conn = sqlite3.connect('bot_database.db')
cursor = conn.cursor()

# Ваш SQL запрос здесь
cursor.execute("SELECT * FROM upload_stats")
print(cursor.fetchall())

# Сохранить изменения
conn.commit()
conn.close()
```

## 📊 Структура базы данных

### Таблица: `upload_stats` (Статистика загрузок)

| Колонка | Тип | Описание |
|---------|-----|----------|
| `id` | INTEGER | Уникальный ID записи (автоинкремент) |
| `admin_id` | INTEGER | ID админа, который загрузил |
| `channel_id` | TEXT | ID канала, куда загружено |
| `title` | TEXT | Название аниме |
| `season` | INTEGER | Номер сезона |
| `episode` | INTEGER | Номер серии |
| `file_id` | TEXT | ID файла в Telegram |
| `message_id` | TEXT | ID сообщения в канале |
| `uploaded_at` | TIMESTAMP | Дата и время загрузки |

### Таблица: `admins` (Администраторы)

| Колонка | Тип | Описание |
|---------|-----|----------|
| `user_id` | INTEGER | Telegram ID пользователя |
| `username` | TEXT | Username или имя |
| `added_at` | TIMESTAMP | Дата добавления |

### Таблица: `channels` (Каналы)

| Колонка | Тип | Описание |
|---------|-----|----------|
| `channel_id` | TEXT | ID канала в Telegram |
| `channel_name` | TEXT | Название канала |
| `added_at` | TIMESTAMP | Дата добавления |

### Таблица: `admin_channels` (Связь админов и каналов)

| Колонка | Тип | Описание |
|---------|-----|----------|
| `admin_id` | INTEGER | ID админа |
| `channel_id` | TEXT | ID канала |
| `assigned_at` | TIMESTAMP | Дата прикрепления |

### Таблица: `templates` (Шаблоны подписей)

| Колонка | Тип | Описание |
|---------|-----|----------|
| `id` | INTEGER | ID шаблона |
| `name` | TEXT | Название шаблона |
| `template_text` | TEXT | Текст шаблона |
| `created_at` | TIMESTAMP | Дата создания |

### Таблица: `channel_templates` (Связь каналов и шаблонов)

| Колонка | Тип | Описание |
|---------|-----|----------|
| `channel_id` | TEXT | ID канала |
| `template_id` | INTEGER | ID шаблона |
| `assigned_at` | TIMESTAMP | Дата прикрепления |

## 📝 Примеры SQL запросов

### 1. Просмотр статистики

```sql
-- Все загрузки
SELECT * FROM upload_stats;

-- Загрузки конкретного админа
SELECT * FROM upload_stats WHERE admin_id = 863116706;

-- Загрузки в конкретный канал
SELECT * FROM upload_stats WHERE channel_id = '@mychannel';

-- Статистика по админам
SELECT 
    a.username,
    COUNT(*) as total_uploads
FROM upload_stats us
JOIN admins a ON us.admin_id = a.user_id
GROUP BY us.admin_id;

-- Статистика по каналам
SELECT 
    c.channel_name,
    COUNT(*) as total_uploads
FROM upload_stats us
JOIN channels c ON us.channel_id = c.channel_id
GROUP BY us.channel_id;
```

### 2. Редактирование статистики

```sql
-- Изменить количество загрузок (добавить запись)
INSERT INTO upload_stats (admin_id, channel_id, title, season, episode, uploaded_at)
VALUES (863116706, '@mychannel', 'Тестовое аниме', 1, 1, '2026-01-12 20:00:00');

-- Удалить конкретную загрузку
DELETE FROM upload_stats WHERE id = 123;

-- Удалить все загрузки админа
DELETE FROM upload_stats WHERE admin_id = 863116706;

-- Удалить все загрузки в канал
DELETE FROM upload_stats WHERE channel_id = '@mychannel';

-- Обновить данные загрузки
UPDATE upload_stats 
SET title = 'Новое название', season = 2, episode = 5
WHERE id = 123;

-- Изменить админа для загрузки
UPDATE upload_stats 
SET admin_id = 123456789
WHERE id = 123;
```

### 3. Сброс статистики

```sql
-- Удалить ВСЮ статистику (будьте осторожны!)
DELETE FROM upload_stats;

-- Сбросить автоинкремент
DELETE FROM sqlite_sequence WHERE name='upload_stats';

-- Удалить статистику старше определенной даты
DELETE FROM upload_stats 
WHERE uploaded_at < '2026-01-01';
```

### 4. Управление админами

```sql
-- Просмотр всех админов
SELECT * FROM admins;

-- Добавить админа вручную
INSERT INTO admins (user_id, username, added_at)
VALUES (123456789, 'NewAdmin', datetime('now'));

-- Удалить админа
DELETE FROM admins WHERE user_id = 123456789;

-- Изменить username
UPDATE admins 
SET username = 'NewUsername'
WHERE user_id = 123456789;
```

### 5. Управление каналами

```sql
-- Просмотр всех каналов
SELECT * FROM channels;

-- Добавить канал вручную
INSERT INTO channels (channel_id, channel_name, added_at)
VALUES ('@newchannel', 'Новый канал', datetime('now'));

-- Удалить канал
DELETE FROM channels WHERE channel_id = '@mychannel';

-- Переименовать канал
UPDATE channels 
SET channel_name = 'Новое название'
WHERE channel_id = '@mychannel';
```

### 6. Прикрепление каналов к админам

```sql
-- Просмотр прикреплений
SELECT 
    a.username,
    c.channel_name
FROM admin_channels ac
JOIN admins a ON ac.admin_id = a.user_id
JOIN channels c ON ac.channel_id = c.channel_id;

-- Прикрепить канал к админу
INSERT INTO admin_channels (admin_id, channel_id, assigned_at)
VALUES (863116706, '@mychannel', datetime('now'));

-- Открепить канал от админа
DELETE FROM admin_channels 
WHERE admin_id = 863116706 AND channel_id = '@mychannel';

-- Открепить все каналы от админа
DELETE FROM admin_channels WHERE admin_id = 863116706;
```

## 🔧 Практические примеры

### Пример 1: Добавить 10 загрузок админу

```sql
-- Остановите бота перед выполнением!

INSERT INTO upload_stats (admin_id, channel_id, title, season, episode, uploaded_at)
VALUES 
    (863116706, '@mychannel', 'Аниме 1', 1, 1, datetime('now')),
    (863116706, '@mychannel', 'Аниме 1', 1, 2, datetime('now')),
    (863116706, '@mychannel', 'Аниме 1', 1, 3, datetime('now')),
    (863116706, '@mychannel', 'Аниме 1', 1, 4, datetime('now')),
    (863116706, '@mychannel', 'Аниме 1', 1, 5, datetime('now')),
    (863116706, '@mychannel', 'Аниме 2', 1, 1, datetime('now')),
    (863116706, '@mychannel', 'Аниме 2', 1, 2, datetime('now')),
    (863116706, '@mychannel', 'Аниме 2', 1, 3, datetime('now')),
    (863116706, '@mychannel', 'Аниме 3', 1, 1, datetime('now')),
    (863116706, '@mychannel', 'Аниме 3', 1, 2, datetime('now'));
```

### Пример 2: Перенести статистику от одного админа к другому

```sql
-- Перенести все загрузки от админа 111111 к админу 222222
UPDATE upload_stats 
SET admin_id = 222222
WHERE admin_id = 111111;
```

### Пример 3: Исправить ошибки в названиях

```sql
-- Исправить название аниме во всех записях
UPDATE upload_stats 
SET title = 'Правильное название'
WHERE title = 'Неправильное название';

-- Исправить номер сезона
UPDATE upload_stats 
SET season = 2
WHERE title = 'Аниме' AND season = 1;
```

## 📊 Экспорт данных

### Экспорт в CSV

```bash
sqlite3 bot_database.db

.headers on
.mode csv
.output statistics.csv
SELECT * FROM upload_stats;
.quit
```

### Экспорт в Excel (через Python)

```python
import sqlite3
import pandas as pd

conn = sqlite3.connect('bot_database.db')
df = pd.read_sql_query("SELECT * FROM upload_stats", conn)
df.to_excel('statistics.xlsx', index=False)
conn.close()
```

## ⚠️ Важные предупреждения

### ❌ НЕ ДЕЛАЙТЕ:

1. **НЕ редактируйте базу при запущенном боте** - это может привести к повреждению данных
2. **НЕ удаляйте таблицы** - бот перестанет работать
3. **НЕ меняйте структуру таблиц** - это сломает бота
4. **НЕ забывайте делать резервные копии**

### ✅ ДЕЛАЙТЕ:

1. **Останавливайте бота перед редактированием**
2. **Делайте резервные копии**
3. **Проверяйте SQL запросы перед выполнением**
4. **Используйте транзакции для безопасности**

## 🔄 Резервное копирование

### Создание резервной копии

```bash
# Windows
copy bot_database.db bot_database_backup_%date:~-4,4%%date:~-7,2%%date:~-10,2%.db

# Linux/Mac
cp bot_database.db bot_database_backup_$(date +%Y%m%d).db
```

### Восстановление из резервной копии

```bash
# Остановите бота!
copy bot_database_backup.db bot_database.db
# Запустите бота
```

## 🆘 Восстановление после ошибок

Если что-то пошло не так:

1. Остановите бота
2. Удалите поврежденную базу: `del bot_database.db`
3. Восстановите из резервной копии: `copy bot_database_backup.db bot_database.db`
4. Запустите бота

Если резервной копии нет:
1. Удалите `bot_database.db`
2. Запустите бота - создастся новая пустая база
3. Добавьте каналы и админов заново через бота

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота в `bot.log`
2. Убедитесь, что бот остановлен перед редактированием
3. Проверьте синтаксис SQL запросов
4. Восстановите из резервной копии

---

**Версия:** 1.0.0  
**Дата:** 2026-01-12  
**База данных:** SQLite 3
