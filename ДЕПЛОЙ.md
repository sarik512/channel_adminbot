# 🚀 Деплой бота на сервер

## 📋 Подготовка файлов

### Файлы для загрузки на сервер:

**Основные файлы:**
- ✅ `main.py` - основной файл бота
- ✅ `database.py` - работа с базой данных
- ✅ `keyboards.py` - клавиатуры
- ✅ `utils.py` - вспомогательные функции
- ✅ `requirements.txt` - зависимости
- ✅ `.gitignore` - исключения для Git

**Скрипты управления:**
- ✅ `db_manager.py` - менеджер базы данных

**Документация:**
- ✅ `README.md` - основная инструкция
- ✅ `ИНСТРУКЦИЯ_АДМИНЫ.md` - управление админами
- ✅ `НАСТРОЙКА_КАНАЛА.md` - настройка каналов
- ✅ `ИНСТРУКЦИЯ_ШАБЛОНЫ.md` - работа с шаблонами
- ✅ `РАБОТА_С_БАЗОЙ.md` - работа с базой данных
- ✅ `ДОБАВЛЕНИЕ_СТАТИСТИКИ.md` - добавление статистики

### ❌ НЕ загружать на сервер:

- ❌ `.env` - создать на сервере вручную
- ❌ `bot_database.db` - создастся автоматически
- ❌ `bot.log` - создастся автоматически
- ❌ `venv/` - создать на сервере
- ❌ `__pycache__/` - создастся автоматически
- ❌ `.pytest_cache/` - не нужен
- ❌ `add_initial_stats.py` - устарел (используйте db_manager.py)
- ❌ `edit_stats.py` - устарел (используйте db_manager.py)

## 🖥️ Установка на сервер

### Шаг 1: Подключение к серверу

```bash
ssh user@your-server.com
```

### Шаг 2: Создание директории

```bash
mkdir -p ~/telegram_bot
cd ~/telegram_bot
```

### Шаг 3: Загрузка файлов

**Вариант A: Через Git (рекомендуется)**
```bash
git clone https://github.com/your-username/your-repo.git .
```

**Вариант B: Через SCP**
```bash
# На локальном компьютере:
scp -r d:/projects/den_project/* user@your-server.com:~/telegram_bot/
```

**Вариант C: Через FileZilla/WinSCP**
- Подключитесь к серверу
- Загрузите все файлы кроме исключенных

### Шаг 4: Установка Python и зависимостей

```bash
# Проверка Python
python3 --version

# Если Python не установлен (Ubuntu/Debian):
sudo apt update
sudo apt install python3 python3-pip python3-venv

# Создание виртуального окружения
python3 -m venv venv

# Активация
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### Шаг 5: Создание .env файла

```bash
nano .env
```

Содержимое:
```env
BOT_TOKEN=your_bot_token_here
SUPER_ADMIN_ID=your_telegram_id
MAX_FILE_SIZE_MB=100
```

Сохранить: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6: Первый запуск

```bash
# Активировать venv (если не активирован)
source venv/bin/activate

# Запустить бота
python3 main.py
```

Вы должны увидеть:
```
🤖 Бот запускается...
✅ Бот запущен и готов к работе!
```

## 🔄 Автозапуск бота (systemd)

### Создание сервиса

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Содержимое:
```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/home/your_username/telegram_bot
Environment="PATH=/home/your_username/telegram_bot/venv/bin"
ExecStart=/home/your_username/telegram_bot/venv/bin/python3 main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Замените:**
- `your_username` на ваше имя пользователя

Сохранить: `Ctrl+O`, `Enter`, `Ctrl+X`

### Запуск сервиса

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable telegram-bot

# Запустить бота
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot

# Просмотр логов
sudo journalctl -u telegram-bot -f
```

### Управление сервисом

```bash
# Остановить
sudo systemctl stop telegram-bot

# Перезапустить
sudo systemctl restart telegram-bot

# Отключить автозапуск
sudo systemctl disable telegram-bot

# Просмотр логов
sudo journalctl -u telegram-bot -n 100
```

## 📊 Управление базой данных на сервере

### Добавление начальной статистики

```bash
# Остановить бота
sudo systemctl stop telegram-bot

# Запустить менеджер
source venv/bin/activate
python3 db_manager.py

# После завершения запустить бота
sudo systemctl start telegram-bot
```

### Резервное копирование

```bash
# Создать бэкап
cp bot_database.db bot_database_backup_$(date +%Y%m%d).db

# Скачать на локальный компьютер
scp user@your-server.com:~/telegram_bot/bot_database.db ./
```

### Восстановление

```bash
# Остановить бота
sudo systemctl stop telegram-bot

# Восстановить из бэкапа
cp bot_database_backup_20260112.db bot_database.db

# Запустить бота
sudo systemctl start telegram-bot
```

## 🔍 Мониторинг

### Просмотр логов бота

```bash
# Последние 100 строк
tail -n 100 bot.log

# В реальном времени
tail -f bot.log

# Логи systemd
sudo journalctl -u telegram-bot -f
```

### Проверка работы

```bash
# Статус сервиса
sudo systemctl status telegram-bot

# Процессы Python
ps aux | grep python

# Использование ресурсов
htop
```

## 🔧 Обновление бота

### Через Git

```bash
# Остановить бота
sudo systemctl stop telegram-bot

# Обновить код
cd ~/telegram_bot
git pull

# Обновить зависимости (если изменились)
source venv/bin/activate
pip install -r requirements.txt

# Запустить бота
sudo systemctl start telegram-bot
```

### Вручную

```bash
# Остановить бота
sudo systemctl stop telegram-bot

# Загрузить новые файлы через SCP/FileZilla

# Запустить бота
sudo systemctl start telegram-bot
```

## 🆘 Решение проблем

### Бот не запускается

```bash
# Проверить логи
sudo journalctl -u telegram-bot -n 50

# Проверить .env
cat .env

# Проверить права
ls -la

# Запустить вручную для отладки
source venv/bin/activate
python3 main.py
```

### Ошибка "Module not found"

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка "Permission denied"

```bash
chmod +x main.py
chown -R your_username:your_username ~/telegram_bot
```

### База данных повреждена

```bash
# Остановить бота
sudo systemctl stop telegram-bot

# Восстановить из бэкапа
cp bot_database_backup.db bot_database.db

# Или создать новую
rm bot_database.db
python3 main.py  # Создаст новую базу

# Запустить бота
sudo systemctl start telegram-bot
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `sudo journalctl -u telegram-bot -f`
2. Проверьте статус: `sudo systemctl status telegram-bot`
3. Проверьте `.env` файл
4. Проверьте права доступа
5. Попробуйте запустить вручную для отладки

## 📋 Чеклист деплоя

- [ ] Создана директория на сервере
- [ ] Загружены все файлы
- [ ] Установлен Python 3
- [ ] Создано виртуальное окружение
- [ ] Установлены зависимости
- [ ] Создан `.env` файл с правильными данными
- [ ] Бот запускается вручную
- [ ] Создан systemd сервис
- [ ] Сервис запущен и работает
- [ ] Проверена работа через Telegram
- [ ] Настроено резервное копирование

---

**Версия:** 1.0.0  
**Дата:** 2026-01-12  
**Python:** 3.8+
